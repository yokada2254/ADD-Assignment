version: "3.8"
services:
  web:
    image: nginx:1.19.2
    ports:
      - "8080:80"
      - "443:443"
      - "8000:8000"
    volumes:
      - ./code:/var/www/html
      - ./default.conf:/etc/nginx/conf.d/default.conf
    links:
      - php
      - mysql
    restart: always

  php:
    build: ./php-with-ext
    volumes:
      - ./code:/var/www/html
    restart: always

  mysql:
    image: mysql:8.0.21
    ports:
      - "3306:3306"
    volumes:
      - ./mysql:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: 1234
    restart: always